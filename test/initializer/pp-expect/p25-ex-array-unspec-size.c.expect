---- stdout ----
---- START ----
// -*-c++-*-
int x[] = { 1, 3, 5 };

int main()
{
  _Static_assert(sizeof(x) / sizeof(*(x+0)) == 3, "");
  return *(x+0) == 1 && *(x+1) == 3 && *(x+2) == 5 && 1? 0 : 1;
}

---- STOP ----
---- stderr ----
---- exit status ----
Exit 0
